# Decision Log

## Change

grok-archive-bound-verdict

## Decisions

| Date | Decision | Reason | Alternatives Considered |
| --- | --- | --- | --- |
| 2026-09-02 | **Codex r1 BC1 (no real producer path): ACCEPTED, and the premise corrected with evidence.** The producer is documented as choreography outside this repo (`plan.md` §D.2) and enforced as an **on-disk contract** (§D.3). No workflow, no conductor stage, no new script, no `agents/verifier.md` edit. | Codex was right that Round 1 never wrote the producer down. But it is not missing — it already runs. Fact 21: three archived packets record, in the `## Override` reason `record_override` wrote (`scripts/sdd.py:435-444`), the whole-file sha256 of their own `verifier-report.md`, and all three still match `sha256sum` today (`88328932…`, `d25b70b8…`, `ce4fdba2…`). The smoke packet says it outright — "bound by check_verdict.py sha256 88328932… at commit 174f04a…" (`archive/2026-09-01-grok-choreography-smoke/decision-log.md:18`). So the report bytes already arrive byte-exactly and the digest is already in the choreographer's hand at archive time. The only new step is writing that hex to `verifier-report.sha256` instead of into a `--force --reason` string. | (1) Add a GitHub workflow or conductor stage to produce the sidecar — rejected: nothing in this repo transports the *report* either (every `verifier-report.md` entered git in an archive commit: `4bacb6a`, `5622a75`, `7d0e31d`, `5bc5a91`, `472fe09`, `7b4200e`), so it would be inventing a pipeline that does not exist for the larger artifact, and it is out of bounds under the Owner's constraints. (2) Have `sdd.py` write the missing sidecar itself — rejected outright: a gate that manufactures its own evidence is not a gate. (3) Edit `agents/verifier.md` so the verifier emits it — rejected as OQ-3 (two pin records for a prose change) and because the verifier writes nothing by design. |
| 2026-09-02 | **Codex r1 BC2 (matcher under-specified): ACCEPTED in full.** `_VERIFIER_OWNED` is now a **closed matcher over a complete corpus audit** of all eight `tasks.md` in the repo (`plan.md` §E.1), with a test per inventoried wording and explicit negatives (#19a-#19d). Widening it beyond that set requires an Owner decision (`plan.md` must_not_do 23, Step 9a). | "Two examples plus a heuristic" is not a contract, and the audit proved the point: it found a **third heading shape Round 1 had missed** — a bare `## Verification` at `archive/2026-09-01-grok-coplan-linux-discover/tasks.md:26` whose five tasks (`:28-33`) are implementer-run commands (`pytest`, `start_probe.py`, `discover_core()`, `negotiate.py --round 1`, `launchguardian scan`). A `verif`-anchored or "heading named Verification" design would have waived all five — exactly the accidental over-waive Codex predicted. Anchoring on the token `verifier`, never `verif`, is the fix. Result: the two scopes cover 6 of 6 verifier-owned pending tasks and waive 0 of 45 implementer-owned tasks. | (1) Line-only matching — rejected: leaves the smoke-packet section shape blocked, which is 4 of the repo's 6 verifier-owned tasks. (2) Heading-name matching (`## Verification`) — rejected by the audit itself; it waives five implementation tasks. (3) A per-packet opt-in marker file naming the verifier tasks — rejected as a second new artifact and a second thing to forget, for a corpus of six tasks. |
| 2026-09-02 | **Codex r1 BC3 (subprocess without an operational policy): ACCEPTED in full.** `plan.md` §F states when the subprocess runs, that it is **at most one per call and never a loop**, `timeout=30` with a measured justification, the fail-closed mapping for every failure mode, and the exact spawn budget per CLI command. Repeated work is removed by a keyword-only `bound=` parameter on `archive_readiness`, **not** by a cache. | The cheap gates run first, so an unclaimed packet spawns nothing — the current tree (1 active packet, 0 claimed) costs **zero** subprocesses, exactly as today. Measured cost is 26.5 ms per spawn (5 runs, largest archived report) against a 30 s timeout: ~1 130× headroom, and stricter than the precedent it copies, since `scripts/record_verify_bound.py:33-38` runs the same `check_verdict.py` with **no timeout at all**. `bound=` takes `verify` and `triage` from 2 spawns to 1; `archive` keeps 2. | (1) A module-level cache keyed on `(path, mtime_ns, size)` — rejected: stale-prone in this codebase's own tests (test #12 mutates a report inside one process; a same-size mutation in one mtime tick would return a cached `ok=True` for bytes that no longer bind), and module-level mutable state in a pinned governance script is a durable readability cost. (2) A cache keyed on the report's content hash — rejected: it puts a second `hashlib.sha256` in `sdd.py`, the exact thing §B and must_not_do 10 exist to prevent. (3) Collapsing `sdd.py archive` from 2 spawns to 1 by returning the binding from `cmd_verify` — rejected: `main()` uses that return value as the process exit status and `cmd_archive:544` deliberately discards it (fact 8); a wider blast radius than the ~26 ms it saves. Two spawns per archive is the accepted bound. |
| 2026-09-02 | **Timeout kept at `timeout=30`; timeout is a BLOCK, never a pass.** | The work is one 5-6 KB file read, one sha256 and one substring test (`scripts/check_verdict.py:29-43`) — no network, no git, no lock. 30 s sits inside the repo's convention (`scripts/conductor/review.py:67` uses 30; `scripts/start_probe.py:92` and `kernel/brief_complete_engine.py:527` use 60) and gives ~1 130× headroom over the measured 26.5 ms. On expiry: `ok=False`, `unbound-verdict`, and the operator's remedy is an audited `--force --reason` — the right place for a judgement call about a hung machine. | (1) No timeout, matching `record_verify_bound.py` — rejected: a hang would wedge `sdd.py archive` with no diagnostic. (2) A short timeout (1-5 s) — rejected: it converts a loaded machine into a spurious refusal for no safety gain, since the failure direction is identical. |
| 2026-09-02 | **`sdd.py triage` output changes for bound packets, and that is intended** (Codex gap 3). A bound packet whose only pending tasks are verifier-owned moves `IN-PROGRESS` → `ARCHIVE-READY`. | Leaving it in `IN-PROGRESS` would reproduce, inside triage, the very `scripts/sdd.py:259-263` contradiction this packet exists to remove: triage would print "finish the packet, or abandon it" for a packet `archive` accepts without `--force`. No bucket, `_TRIAGE_ORDER` or `_TRIAGE_NEXT` entry is added, removed or reworded; packets with no report bucket byte-for-byte as today. | Suppressing the triage change to keep the diff smaller — rejected: it ships a knowingly self-contradictory CLI, which is the class of defect this packet is about. |
| 2026-09-02 | **Codex's "too ambitious in one shot": NOTED, scope unchanged.** The CLI-output and triage changes stay in this packet. | They are not extras. Facts 5 and 7 are *pre-existing contradictions* that this gate would otherwise make visible and wrong — `cmd_verify:416` can never reach `archive_readiness` for a bound packet, and `cmd_verify:399-400` would hard-code a `--force` claim that archive no longer honors. Splitting them ships a CLI that contradicts itself for exactly the packets this feature creates. The test matrix is large because the fail-closed matrix is large; `plan.md` §F.7 tiers it (unit / binding-contract / end-to-end) so a failure localizes. | Splitting into a core-gate packet plus a CLI packet — rejected for the reason above; the intermediate state is a self-contradictory `sdd.py verify`. |
| 2026-09-02 | **Implementation turn: test #19d's live-packet row reads `(1, 0)`, not the `(0, 5)` recorded in `plan.md` §E.1.** The closed matcher is unchanged; only this packet's own `tasks.md` moved. | §E.1 captured the corpus while `changes/grok-archive-bound-verdict/tasks.md` was still the template verbatim (five pending implementer tasks, zero verifier-owned). Step 14 fills it: the eight implementer boxes are ticked and one pending task remains — the verifier-owned Step 12, written in the **inventoried line form** (`- [ ] Invoke the verifier subagent; do not self-certify.`, the wording at `archive/2026-09-02-grok-coplan-closure-gate/tasks.md:47`). So `verifier_owned_pending` correctly returns `(1, 0)`, and #19d asserts the tree as it actually is at the end of this turn. **No new wording entered the repo** — Step 9a's re-run of the inventory found every heading and every `verif` hit already in §E.1 — so `_VERIFIER_OWNED` was not widened and must_not_do 23 holds. | (1) Leave `tasks.md` as the template so the `(0, 5)` row stays literally true — rejected: it would mean shipping an unfilled packet to keep a test constant, and Step 14 requires the real implementer record. (2) Drop the live packet from #19d's corpus — rejected: it is the row most likely to drift, and dropping it would weaken the regression exactly where the next packet will land. |
| 2026-09-02 | **No migration or backfill** (Codex gap 2), and the six archived reports get no sidecars. | `cmd_status:305-306`, `cmd_triage:357` and `cmd_verify:381` all resolve `sdd-plus/changes/` only; nothing walks `sdd-plus/archive/`, so archived packets cannot be reclassified by this change (fact 16). An in-flight packet with a report but no sidecar behaves exactly as today — a missed benefit, never a false pass (OQ-2) — and its remedy is one `sha256sum` line. | Retrofitting sidecars into `sdd-plus/archive/` — rejected: it rewrites history for artifacts no predicate can reach. |
