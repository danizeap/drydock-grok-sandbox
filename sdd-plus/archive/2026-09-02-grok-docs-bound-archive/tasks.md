# Tasks

## Change

grok-docs-bound-archive

## Implementation

- [x] Step 0: re-read README, PROJECT_CONTEXT, scope-contract, `sdd.py:27-29` and `cmd_archive`
      `:773-807`; re-prove the sidecar hash and the zero-`## Override` count.
- [x] Step 1: append the bound-archive paragraph to `README.md`.
- [x] Step 2: add the bound-archive paragraph after `PROJECT_CONTEXT.md:21`.
- [x] Step 3: extend `PROJECT_CONTEXT.md:60` with the transport/archive definition clause.
- [x] Step 4: run the read-only checks 4a–4e (and 4f only if the yaml was edited); paste output into
      `verification.md`. **No pytest, no `start_probe.py`.**
- [x] Step 5: fill `decision-log.md`, tick these boxes, record the Result. Stop before commit.
