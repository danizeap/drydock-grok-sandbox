# Decision Log

## Change

grok-refuse-brief-engine

## Round 2 of 2 (final) — Pilot audit of Codex round-1 critique

Codex ran round 1 as `gpt-5.4-mini` ("light task -> workhorse model"), transport only — the
choreographer did not audit its output. Source read verbatim from disk:
`negotiate-grok-refuse-brief-engine-r1.json`. Codex is a peer; its critique is input, not orders.
Each blocking concern below was independently re-checked against the tree at HEAD `0b4a475` before
being dispositioned, and the checks are recorded so the Owner can re-run them.

## Decisions

| Date | Decision | Reason | Alternatives Considered |
| --- | --- | --- | --- |
| 2026-09-02 | **BC1 — "plan silently broadens behavior by refusing `--record-verify=…` and abbreviations": PARTIAL ACCEPT.** Accept the factual finding and rewrite the framing; reject the implied remedy of reverting to the literal matcher. OQ-1's recorded default (strict matcher) stands. | Codex is factually right, and round 1 understated it. Verified live and side-effect-free (`BAD_NAME` fails the kebab regex at `kernel/brief_engine.py:518-519`, returning before any subprocess or `append_event`): `python3 kernel/brief.py --record-verify=BAD_NAME` and `python3 scripts/brief.py --record-ver BAD_NAME` today reach `record_verify()` and exit **0**; under the strict overlay they become exit **1** `bare-record-verify-refused`. So two files listed as "explicitly not changed" do change observable behavior, including exit codes. Round 1's Mode section ("no contract change") and its §2 ("bonus") could not both stand — accepted, both rewritten. The remedy is rejected because (a) OQ-1's strict default was already recorded and Owner intent for this packet forbids silently reversing a recorded default on a peer's say-so; (b) new fact 12 — a repo-wide grep for any `--record…` spelling finds exactly one executing caller, `scripts/record_verify_bound.py:55`, using the canonical two-token form, plus tests (canonical) and docstrings — so the strict matcher breaks **zero** known callers and the only behavior that changes is the bypass itself; (c) every delta moves in the refuse direction, so `append_event` never gets easier to reach. | (1) Revert to the literal `"--record-verify" in argv` matcher — rejected as the default; retained as the *stated fallback* under OQ-1, with the residual to be recorded in `sdd-plus/security/accepted-risks.md` if chosen. (2) A third matcher design (e.g. exact-set allowlist, or matching only `=` and not abbreviations) — **not considered**; Owner intent forbids inventing one and Codex proposed none. (3) Edit `kernel/brief.py` and `scripts/brief.py` to apply the strict matcher in-process — rejected: it would edit and re-pin two more files, widening a LITE packet, for the same end state the delegation edge already produces. (4) Keep "no contract change" and argue the bypass isn't a contract — rejected as dishonest; the exit code visibly changes. |
| 2026-09-02 | **BC2 — "move/overwrite sequence too easy to perturb without a mechanical safeguard": PARTIAL ACCEPT.** Reject the premise that no safeguard exists; accept that the round-1 wording was perturbable, and tighten order/stop-rules **without adding machinery**. | Round 1 already specified move-first/hash-then-edit (§1, Steps 2–4), already forbade `git mv` and any edit to the moved file (must_not_do 1, 5), and already had `test_completeness_engine_bytes_unmoved` plus `check_pins()` as mechanical nets — Codex did not credit these, so the premise is rejected. But three real wording gaps were confirmed and are accepted: (i) "stop unless `aa3ba09…`" never defined what *stop* means, so Step 3 now specifies abort semantics — delete the copy, change nothing else, report to Owner, do not diagnose-and-continue; (ii) the hash was proven **before** the overlay overwrite but never after, and the overwrite is precisely the operation that can hit the wrong path and clobber the frozen file — new Step 4a re-runs the identical `sha256sum` immediately after it; (iii) the pinned value was assumed current rather than proven current — new Step 8 re-asserts the hash immediately before `drydock-pins.json` is written. Also promoted "never open that file" from prose into a hard tool-level rule: after Step 3 no `Edit`/`Write`/`sed`/formatter may name `kernel/brief_complete_engine.py` for any reason, including fixing its stale docstring. | (1) A helper script / pin tool / `Makefile` target / pre-commit addition to mechanize the copy-and-verify — **rejected** per Owner intent: that invents a new product inside a LITE packet. Recorded as must_not_do 19. (2) `git mv` plus recreating the old path — rejected: `kernel/brief_engine.py` must never stop existing (`kernel/brief.py:23`, `scripts/brief.py:21`, pin line 25). (3) Making the moved file read-only via `chmod` — rejected: a mode bit is not tracked by the pin, adds a state the rollback must undo, and does not stop a `Write` that recreates the file. (4) Doing nothing, on the grounds the choreography was already correct — rejected: the wording gaps are real even though the premise was overstated. |
| 2026-09-02 | **Codex gap 1 (abbreviation parity unstated): ACCEPTED.** Parity is intentionally dropped; the plan now says so in one place with the evidence. | It was genuinely ambiguous in round 1 — fact 11 showed the table but no sentence declared the intent. Now stated in Mode, §Behavior deltas on unedited files, and §OQ-1. | Leaving it implicit — rejected; it is the exact ambiguity BC1 formed around. |
| 2026-09-02 | **Codex gap 2 (no acceptance criterion for the extra `execv` hop): ACCEPTED.** Added verification command 3a (wrapper path: `diff <(python3 scripts/brief.py) <(python3 kernel/brief_complete_engine.py)` must be empty) and 4a (bound path: correct digest must yield a completeness-side reason, proving `check_verdict` was passed and the moved engine reached). | "Tests pass" was the only criterion for the hop, and the hop is the part least covered by unit tests. Both checks are cheap, read-only, and use `does-not-exist` / `/tmp` so no ledger event can be minted. | Adding a timing/perf assertion — rejected as noise at this scale. Relying on `tests/test_brief_wrapper.py:41-45` alone — rejected: it proves the hop returns 0, not that output is unchanged. |
| 2026-09-02 | **Codex gap 3 (no way to tell a real fix from a relocated hole): ACCEPTED.** Added §Review criterion — four pass/fail checks a reviewer can apply without reading the diff line by line. | §Risks already admitted the residual honestly, but admission is not a criterion. Criterion 2 is the load-bearing one: after the change, `brief_complete_engine` may appear only in the gate, the tests, the pins, and the overlay docstring — if any user-facing doc or wrapper acquires that path, the hole was relocated, not closed. | Leaving it to the verifier subagent's judgement — rejected: the distinction is the whole point of the packet and should not depend on who reviews it. |
| 2026-09-02 | **Codex risk "stricter matcher as a future maintenance trap": ACCEPTED as a risk, no design change.** Recorded in §Risks with its resolution direction. | Real but fail-safe. If a future option shrinks argparse's prefix space (e.g. `--record-hash` makes `--record` ambiguous), argparse exits 2 while the overlay refuses with exit 1 — both non-zero, neither records. The matcher can become over-broad, never under-broad, and over-broad means refusing a token the parser would itself reject. | Pre-emptively narrowing the matcher to an explicit spelling list — rejected: that is the forbidden third design, and it would silently reopen the bypass for any spelling not enumerated. |
| 2026-09-02 | **Codex risks already covered (hole relocated not removed; stale docstring left deliberately; pin/probe ordering; `execv` debuggability; byte-change fragility): NO CHANGE.** | All five were already in round 1 §Risks with mitigations. Re-checked; still accurate. | — |
| 2026-09-02 | **Codex `decomposition` (six tasks with `owner`/`tier` assignments): NOT ADOPTED as work allocation.** Its *content* is already covered by Steps 1–12; its owner/tier column is ignored. | The decomposition is a reasonable restatement of the plan's own steps and adds nothing to sequencing. Assigning implementation ownership to a peer model is not Codex's call, and this turn is plan-only regardless. | Restructuring Steps to mirror the decomposition — rejected: the existing order encodes the byte-freeze choreography, which the decomposition's ordering does not. |
| 2026-09-02 | **Owner decision — accept the residual and build the plan as written.** Overlay the advertised path `kernel/brief_engine.py`; keep the completeness bytes frozen at sha256 `aa3ba09…` and callable via `scripts/record_verify_bound.py` *after* `check_verdict`, without killing the completeness CLI on the moved file; **OQ-1 resolves to its stated default — the STRICT matcher** (the overlay refuses `--record-verify=form` and unambiguous abbreviations); leftover hole 3 is a later packet, not this one; no client trees are involved. | The Owner reviewed the round-2 framing (BC1's contract-change correction and fact 12's zero-known-callers bound) and accepted the trade: the residual is one unadvertised path, every advertised path refuses, and the tightening on the two unedited wrappers breaks no caller in this repo. Silence would have meant the default anyway; this records it as an explicit decision so the strict matcher is not later read as an implementation liberty. Implementation proceeds under this decision and the Result stays Pending until independent verification. | (1) The literal-copy fallback of §OQ-1 — rejected by the Owner; it would leave the equals/abbreviation bypass as a documented, unfixed residual across all four entry points. (2) Removing the bare `--record-verify` mode from the moved completeness file — rejected: it would break the `aa3ba09…` freeze, which is the constraint the whole packet is built around, and would break the bound path that `record_verify_bound.py` depends on. (3) Bundling leftover hole 3 into this packet — rejected as scope widening on a LITE packet. |
| 2026-09-02 | **Packet hygiene: stripped stray `</content>\n</invoke>` trailer bytes from `plan.md` and `brief.md`.** | Both files ended with leftover tool-call markup from the round-1 authoring turn — malformed Markdown in governed artifacts. Removal is inside the packet dir and touches no production file. | Leaving them — rejected; they would end up in the archived packet. |

## Standing constraints reaffirmed this round

- Completeness bytes stay frozen at sha256
  `aa3ba09fa5b8bd3a861d3ac3a58990a06251d0204c5632f93b6658ad85368a2b`. Confirmed present on
  `kernel/brief_engine.py` at HEAD `0b4a475` this turn. Completeness is never rewritten.
- Scope is leftover hole 2 only. Holes 1/3/4, `hooks/packet_guard.py`, `.github/workflows/`,
  `scripts/conductor/*` are untouched, and no client trees are involved.
- Negotiation cap is 2 and round 2 is final. There is no round 3 in which to settle OQ-1 by
  further exchange; it settles by the stated default or by the Owner.

## Override — 2026-09-02
- Gates waived: 1 pending task(s); unfilled placeholders in verification.md
- Reason: independent VERIFIED WITH NOTES @ cf0bf36 sha256 ce4fdba20cf5f2827b430e3218c614423d89383607b40c1f972620c4bfc6e184; Owner --force because Result was Pending
